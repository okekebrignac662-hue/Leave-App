# 📊 คู่มือการเชื่อมต่อระบบลางานกับ Google Sheets (Leave App & Google Sheets Integration)

ระบบนี้รองรับการบันทึกและซิงค์ข้อมูลคำขอลางานและรายชื่อพนักงานไปยัง **Google Sheets** โดยอัตโนมัติผ่าน **Google Apps Script Webhook** ซึ่งเป็นวิธีที่ง่าย สะดวก และไม่มีค่าใช้จ่าย ไม่จำเป็นต้องเปิดบัญชี Google Cloud Console หรือตั้งค่า Service Account ให้ยุ่งยาก

---

## ✨ ความสามารถของระบบ (Features)

1. **บันทึกคำขอลางานอัตโนมัติแบบ Real-time**:
   - เมื่อพนักงานยื่นคำขอลางาน (ทั้งลาเต็มวันและลารายชั่วโมง) ข้อมูลจะถูกบันทึกลง Google Sheets ทันที
   - แถวข้อมูลจะมีแถบสีสถานะอัตโนมัติ เช่น ⏳ **รออนุมัติ (สีส้ม/เหลือง)**
2. **อัปเดตสถานะอัตโนมัติ (Status Sync)**:
   - เมื่อหัวหน้างาน **อนุมัติ (Approved)**: ช่องสถานะเปลี่ยนเป็นสีเขียว พร้อมชื่อผู้อนุมัติและเวลา
   - เมื่อหัวหน้างาน **ไม่อนุมัติ (Rejected)**: ช่องสถานะเปลี่ยนเป็นสีแดง พร้อมบันทึกเหตุผลที่ไม่อนุมัติ
   - เมื่อพนักงาน **ยกเลิกคำขอ (Cancelled)**: ช่องสถานะเปลี่ยนเป็นสีเทา
3. **บันทึกข้อมูลพนักงานแยกแผ่นงาน (Employees Sheet)**:
   - บันทึกรายชื่อพนักงาน, แผนก, กะการทำงาน (A / B / เช้าตลอด), สิทธิ์การใช้งาน, PIN, และโควตาวันลาเริ่มต้น
4. **ปุ่มกดซิงค์ข้อมูลทั้งหมดด้วยมือ (Manual Bulk Sync)**:
   - ซิงค์ข้อมูลทั้งหมด (คำขอลางาน + รายชื่อพนักงาน)
   - ซิงค์เฉพาะคำขอลางาน
   - ซิงค์เฉพาะรายชื่อพนักงาน

---

## 📑 โครงสร้าง 2 แผ่นงานที่จะถูกสร้างขึ้นอัตโนมัติ

| แผ่นงาน | ข้อมูลที่จัดเก็บ |
|---|---|
| **1. รายการลางาน** | รหัสคำขอ, รหัสพนักงาน, ชื่อ-นามสกุล, แผนก, กะการทำงาน, ประเภทการลา, รูปแบบการลา, วันเริ่มต้น, วันสิ้นสุด, จำนวนวัน, เวลาเริ่ม, เวลาสิ้นสุด, จำนวนชั่วโมง, เหตุผลการลา, สถานะ, ผู้พิจารณา, วันที่พิจารณา, เหตุผลที่ไม่อนุมัติ, วันที่ยื่นคำขอ, อัปเดตล่าสุด |
| **2. ข้อมูลพนักงาน** | รหัสพนักงาน, ชื่อ-นามสกุล, แผนก, กะการทำงาน, บทบาท (Role), รหัสผ่าน (PIN), โควตาพักร้อน, โควตากิจ, โควตาป่วย, โควตาไม่รับค่าจ้าง, วันที่ลงทะเบียน, อัปเดตล่าสุด |

---

## 🚀 ขั้นตอนการติดตั้ง (ใช้เวลาประมาณ 2 นาที)

### ขั้นตอนที่ 1: สร้าง Google Sheet ใหม่
1. เปิด [Google Sheets](https://sheets.google.com/) แล้วกดสร้าง **สเปรดชีตว่างเปล่า (Blank spreadsheet)** ใหม่
2. ตั้งชื่อไฟล์สเปรดชีตตามต้องการ เช่น `ระบบบันทึกข้อมูลการลางาน 2026`

### ขั้นตอนที่ 2: เปิด Google Apps Script
1. ที่แถบเมนูด้านบนของ Google Sheet ให้คลิกเมนู **"ส่วนขยาย" (Extensions)**
2. เลือก **"Apps Script"** (หน้าต่างเขียนโค้ดจะเปิดขึ้นมาในแท็บใหม่)

### ขั้นตอนที่ 3: วางโค้ดสคริปต์
1. ลบโค้ดเดิมในหน้าต่าง `Code.gs` ออกทั้งหมด
2. คัดลอกโค้ดจากไฟล์ `google-apps-script.js` ในโฟลเดอร์ของโปรเจกต์ (หรือกดปุ่ม **"📋 คัดลอกโค้ดสคริปต์"** ในหน้าเว็บแท็บ Google Sheets)
3. วางโค้ดลงในหน้าต่าง Apps Script
4. กดปุ่ม **บันทึก** (รูปแผ่นดิสก์ 💾 หรือกด `Ctrl + S`)

### ขั้นตอนที่ 4: การ Deploy เป็น Web App (สำคัญมาก ⚠️)
1. คลิกปุ่มสีน้ำเงิน **"การทำให้ใช้งานได้" (Deploy)** ที่มุมบนขวา
2. เลือก **"การทำให้ใช้งานได้ใหม่" (New deployment)**
3. คลิกที่รูปเฟือง ⚙️ ด้านซ้าย เลือกประเภทเป็น **"เว็บแอป" (Web app)**
4. ตั้งค่าดังต่อไปนี้:
   - **คำอธิบาย (Description)**: `Leave App Webhook`
   - **ดำเนินการในฐานะ (Execute as)**: `ตัวฉัน (Me)`
   - **ผู้มีสิทธิ์เข้าถึง (Who has access)**: **`ทุกคน (Anyone)`** *(⚠️ สำคัญมาก ต้องเลือก Anyone เพื่อให้เซิร์ฟเวอร์สามารถส่งข้อมูลเข้ามาได้)*
5. คลิกปุ่ม **"ทำให้ใช้งานได้" (Deploy)**
6. หากระบบขอสิทธิ์เข้าถึง ให้คลิก **"ให้สิทธิ์การเข้าถึง" (Authorize access)** > เลือกบัญชี Google ของคุณ > คลิก **"Advanced"** > คลิก **"Go to Leave App Webhook (unsafe)"** > คลิก **"Allow"**
7. คัดลอก **URL ของเว็บแอป (Web app URL)** ที่ลงท้ายด้วย `/exec`
   *(ตัวอย่าง: `https://script.google.com/macros/s/AKfycbx.../exec`)*

### ขั้นตอนที่ 5: นำ URL มาใส่ในระบบ Leave App
1. เข้าสู่ระบบ Leave App ด้วยบัญชีผู้ดูแลระบบ (Admin) เช่น `ADMIN-001`
2. ไปที่แท็บ **"📊 Google Sheets"**
3. วาง Webhook URL ลงในช่อง **"Google Webhook URL"**
4. คลิกปุ่ม **"💾 บันทึก URL"** และคลิกปุ่ม **"🧪 ทดสอบการเชื่อมต่อ"**
5. เมื่อขึ้นเครื่องหมายถูกสีเขียว ✓ ระบบจะสร้างหัวตารางใน Google Sheet ให้โดยอัตโนมัติ และพร้อมใช้งานทันที!

---

## 🔄 การทดสอบและการซิงค์ข้อมูล (Manual Sync)

- หากคุณต้องการนำเข้าข้อมูลคำขอเดิมทั้งหมดและพนักงานที่มีอยู่แล้วในระบบ ให้คลิกปุ่ม **"ซิงค์ทั้งหมดทันที" (Sync All)** ข้อมูลทั้งหมดจะถูกเขียนลง Google Sheet ทันทีในครั้งเดียว
- คุณสามารถเปิด/ปิด การบันทึกข้อมูลอัตโนมัติ (Auto-Sync) ได้ตลอดเวลาผ่านสวิตช์ Toggle ในแท็บ Google Sheets

---

## 🛠️ การแก้ไขปัญหาที่พบบ่อย (Troubleshooting)

| ปัญหา | สาเหตุ | วิธีแก้ไข |
|---|---|---|
| **ทดสอบการเชื่อมต่อแล้วขึ้น 401 หรือ Forbidden** | ตอน Deploy เลือกสิทธิ์เข้าถึงผิด | ให้กด Deploy > Manage deployments > แก้ไข Web app ให้ "Who has access" เป็น **"Anyone"** |
| **บันทึกข้อมูลแล้วไม่ขึ้นใน Sheet** | อาจมีการแก้ไขโค้ดแต่ไม่ได้ New Deployment | ใน Google Apps Script ทุกครั้งที่มีการแก้ไขโค้ด ต้องกด Deploy > Manage deployments > Edit > New version แล้วกด Deploy ซ้ำ |
| **เซิร์ฟเวอร์แจ้งเตือน Timeout** | สัญญาณอินเทอร์เน็ตหรือ Google มีการประมวลผลนาน | ฟังก์ชันของระบบทำงานแบบ Asynchronous ข้อมูลการขอลางานในระบบยังคงบันทึกสำเร็จตามปกติ และจะซิงค์ใหม่อัตโนมัติเมื่อกด Sync All |
